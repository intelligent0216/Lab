#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int get_power(int number, int power)
{
  int x = number;
  for (int q = 1; q < power; q++)
  {
    x*=number;
  }
  return x;
}

int main(void)
{
  return 0;
}
