#include <stdio.h>
#include <malloc.h>
int len_arr = 1;

int* get_simples(int count)
{
int arr[count];
int *arr2 = NULL;

for (int q = 0; q < count; q++)
{
arr[q] = 0;
}

for (int q = 2; q*q < count; q++)
if (arr[q] == 0)
for (int w = q*q; w < count; w += q)
arr[w] = 1;

for (int q = 2; q < count; q++)
{
if (arr[q] == 0) {
arr2 = (int*)realloc(arr2, (len_arr) * sizeof(int));
arr2[len_arr - 1] = q;
len_arr++;
}
}

return arr2;
}


int main(void) {
int count;
int *arr;
scanf("%d", &count);
arr = get_simples(count);
for (int q = 0; q < len_arr - 1; q++)
  printf("%d ", arr[q]);
printf("\n");

return 0;
}
